<div class="deferred-block deferred-block--carousel" data-block="product-carousel">
    <div class="deferred-block__header">
        <div>
            <p class="deferred-block__eyebrow">HTML + tiny client module</p>
            <h3 class="deferred-block__title">Product rail</h3>
        </div>
        <span class="deferred-block__badge">Try Prev / Next</span>
    </div>
    <p class="deferred-block__summary">The server streams the finished card markup. After arrival, a tiny scoped module turns it into a carousel without taking over the page.</p>
    <div class="carousel" data-carousel>
        {% for product in products %}
        <div class="carousel__item">
            <div class="product-card">
                <div class="product-card__name">{{ product.name }}</div>
                <div class="product-card__price">${{ product.price }}</div>
                <span class="badge badge--{{ product.status }}">{{ product.status }}</span>
            </div>
        </div>
        {% else %}
        <p class="deferred-block__empty">No products to display.</p>
        {% endfor %}
    </div>
    {% if products|length > 1 %}
    <div class="carousel__controls">
        <button class="btn btn--secondary" data-carousel-prev>← Prev</button>
        <button class="btn btn--secondary" data-carousel-next>Next →</button>
    </div>
    {% endif %}
</div>
