<div class="preview-card preview-card--nested">
  <p class="preview-card__eyebrow">Low-Level Escape Hatch</p>
  <h3 class="preview-card__title">When resource-level reads are acceptable</h3>
  <div class="preview-checklist">
    {% for rule in rules %}
      <div class="preview-checklist__item">
        <span class="preview-checklist__mark" aria-hidden="true">+</span>
        <p>{{ rule }}</p>
      </div>
    {% endfor %}
  </div>
</div>
