<div class="preview-card preview-card--nested graphql-derived-notes">
  <p class="preview-card__eyebrow">GraphQL-First</p>
  <h3 class="preview-card__title">What this page is proving</h3>
  <div class="preview-table">
    <table>
      <thead>
        <tr>
          <th>Question</th>
          <th>Semitexa answer</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Do I need a public REST route first?</td>
          <td>No. The public story can be <code>POST /graphql</code> from day one.</td>
        </tr>
        <tr>
          <td>Does GraphQL push me into resolver classes everywhere?</td>
          <td>No. The use case still hangs on explicit payload and output contracts.</td>
        </tr>
        <tr>
          <td>Where does the response shape live?</td>
          <td>In a typed output DTO instead of improvised arrays and field-by-field resolver glue.</td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
