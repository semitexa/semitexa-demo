<div class="preview-card preview-card--nested">
  <p class="preview-card__eyebrow">Destructive Safety</p>
  <h3 class="preview-card__title">Why column drops are intentionally slow</h3>
  <div class="preview-checklist">
    {% for rule in rules %}
      <div class="preview-checklist__item">
        <span class="preview-checklist__mark" aria-hidden="true">+</span>
        <p>{{ rule }}</p>
      </div>
    {% endfor %}
  </div>
</div>
