{% include '@project-layouts-semitexa-demo/components/feature-tree.html.twig' with {
  sections: featureTree|default([]),
  currentSection: currentSection|default(null),
  currentSlug: currentSlug|default(null),
} only %}
